<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDisplayName</key>
    <string>PAX Controller</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleName</key>
    <string>PaxController</string>
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>UILaunchScreen</key>
    <dict/>

    <!-- Bluetooth permissions — REQUIRED by Apple -->
    <key>NSBluetoothAlwaysUsageDescription</key>
    <string>This app needs Bluetooth access to communicate with your vaporizer device.</string>
    <key>NSBluetoothPeripheralUsageDescription</key>
    <string>This app needs Bluetooth access to communicate with your vaporizer device.</string>

    <!-- Keeps the CoreBluetooth session (and auto-reconnect) alive while the
         app is backgrounded or the phone is locked, and lets iOS relaunch the
         app into the background on a BLE event via state restoration. Does
         NOT survive the user force-quitting the app — no iOS API allows that. -->
    <key>UIBackgroundModes</key>
    <array>
        <string>bluetooth-central</string>
    </array>

    <!-- Live Activity (Lock Screen + Dynamic Island) support -->
    <key>NSSupportsLiveActivities</key>
    <true/>

    <!-- Supported iOS versions. 16.2 is the floor for the non-deprecated
         ActivityKit content API used by the Lock Screen card. -->
    <key>MinimumOSVersion</key>
    <string>16.2</string>

    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>bluetooth-le</string>
        <string>armv7</string>
    </array>

    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <key>UIApplicationSceneManifest</key>
    <dict>
        <key>UIApplicationSupportsMultipleScenes</key>
        <false/>
    </dict>
</dict>
</plist>
