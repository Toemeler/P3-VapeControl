# Simulator screenshots

Captured automatically by the `Build` workflow.

- Device: iPhone 14
- Runtime: iOS 26.2
- Run: 34980835389

The simulator has no Bluetooth radio, so the app is launched with
a demo fixture standing in for a connected PAX 3. These confirm the
app builds, launches and renders; they do not exercise the PAX
connection.
